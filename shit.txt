maybe think about changing collection/subcollection reference data as its own type (instead of object type), so that dataurl can be fetched automatically
chapter sharded counters
    implement on view
    implement on functions